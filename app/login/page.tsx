'use client';
import { FormEvent, useEffect, useState } from 'react';
export default function LoginPage() { const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [rememberMe,setRememberMe]=useState(true); const [error,setError]=useState(''); const submit=async(e:FormEvent)=>{e.preventDefault();setError('');const response=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password,rememberMe})});if(response.ok) window.location.href='/contests'; else setError((await response.json()).error||'Unable to sign in.');}; return <main className="shell" style={{maxWidth:480,paddingTop:80}}><div className="eyebrow">Private league</div><h1>CFB <span style={{color:'var(--blue)'}}>PICKS</span></h1><p>Sign in to choose your contest.</p><form onSubmit={submit} style={{display:'grid',gap:14,marginTop:28}}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="username" required style={{display:'block',width:'100%',padding:14,marginTop:6}} /></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required style={{display:'block',width:'100%',padding:14,marginTop:6}} /></label><label style={{display:'flex',alignItems:'center',gap:8}}><input type="checkbox" checked={rememberMe} onChange={e=>setRememberMe(e.target.checked)} /> Remember me</label><button type="submit" className="tab active">Sign in</button>{error&&<p style={{color:'#b04b4b'}}>{error}</p>}</form><p style={{marginTop:24}}><a href="/register">Create a new account</a></p></main>; }
