'use client';
import { FormEvent, useEffect, useState } from 'react';
export default function RegisterPage(){const [usernames,setUsernames]=useState<string[]>([]);const [email,setEmail]=useState('');const [name,setName]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');useEffect(()=>{fetch('/api/auth/usernames').then(response=>response.json()).then(result=>setUsernames(result.usernames??[])).catch(()=>setError('Usernames could not be loaded.'));},[]);const submit=async(event:FormEvent)=>{event.preventDefault();setError('');const response=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,displayName:name,password})});if(response.ok)window.location.href='/contests';else setError((await response.json()).error||'Account could not be created.');};return <main className="shell" style={{maxWidth:480,paddingTop:80}}><div className="eyebrow">Private league</div><h1>Create account</h1><p>Use the username you already use in the league, then choose your login credentials.</p><form onSubmit={submit} style={{display:'grid',gap:14,marginTop:28}}><label>Email<input type="email" value={email} onChange={event=>setEmail(event.target.value)} autoComplete="username" required style={{display:'block',width:'100%',padding:14,marginTop:6}} /></label><label>League username<select value={name} onChange={event=>setName(event.target.value)} required style={{display:'block',width:'100%',padding:14,marginTop:6}}><option value="">Choose your username</option>{usernames.map(username=><option key={username}>{username}</option>)}</select></label><label>Password<input type="password" value={password} onChange={event=>setPassword(event.target.value)} autoComplete="new-password" minLength={8} required style={{display:'block',width:'100%',padding:14,marginTop:6}} /></label><button type="submit" className="tab active">Create account</button>{error&&<p style={{color:'#b04b4b'}}>{error}</p>}</form><p style={{marginTop:24}}><a href="/login">Already have an account? Sign in.</a></p></main>;}
