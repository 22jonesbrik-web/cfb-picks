'use client';
import { useState } from 'react';
export default function SubmitPicks({ weekId, complete }: { weekId: string; complete: number }) { const [tieBreakerTotal,setTieBreakerTotal]=useState(''); const [message,setMessage]=useState(''); const [busy,setBusy]=useState(false); const submit=async()=>{setBusy(true);setMessage('');const response=await fetch('/api/picks/submit',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({weekId,tieBreakerTotal:Number(tieBreakerTotal)})});const result=await response.json();setMessage(response.ok?'Picks submitted.':result.error||'Picks could not be submitted.');setBusy(false);}; return <div style={{display:'flex',alignItems:'center',gap:12,flexWrap:'wrap',margin:'18px 0'}}><label>Tiebreaker total <input type="number" min="0" value={tieBreakerTotal} onChange={event=>setTieBreakerTotal(event.target.value)} placeholder="Total points" style={{width:110,padding:8,marginLeft:6}} /></label><button className="tab active" onClick={submit} disabled={busy||complete!==10||tieBreakerTotal===''}>{busy?'Submitting...':'Submit 10 Picks'}</button><small>{complete===10?'Enter the final-score total tiebreaker.':'Select exactly 10 games before submitting.'}</small>{message&&<small role="status">{message}</small>}</div>; }
