'use client';
import { FormEvent, useState } from 'react';
export default function ManageAccount(){const [password,setPassword]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);const submit=async(event:FormEvent)=>{event.preventDefault();setBusy(true);const response=await fetch('/api/manage/password',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({password})});const result=await response.json();setMessage(response.ok?'Password updated.':result.error||'Password could not be changed.');if(response.ok)setPassword('');setBusy(false);};return <form onSubmit={submit} style={{display:'grid',gap:10,maxWidth:420}}><label>New password<input type="password" minLength={8} value={password} onChange={event=>setPassword(event.target.value)} autoComplete="new-password" required style={{display:'block',width:'100%',padding:14,marginTop:6}} /></label><button className="tab active" disabled={busy}>{busy?'Updating...':'Change password'}</button>{message&&<small>{message}</small>}</form>;}
